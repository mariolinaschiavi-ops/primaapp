<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Audio Guida - Equazioni di Primo Grado</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f2f7ff;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        header {
            background-color: #2e6cff;
            color: white;
            padding: 15px;
            font-size: 1.5em;
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
        }

        section {
            margin: 20px auto;
            width: 90%;
            max-width: 800px;
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        h2 {
            color: #2e6cff;
        }

        button {
            background-color: #2e6cff;
            color: white;
            border: none;
            padding: 10px 18px;
            margin: 8px;
            font-size: 1em;
            border-radius: 8px;
            cursor: pointer;
            transition: 0.3s;
        }

        button:hover {
            background-color: #1c4cd3;
        }

        .exercise {
            margin-top: 15px;
            padding: 15px;
            background: #eef4ff;
            border-radius: 8px;
            text-align: left;
        }

        .feedback {
            font-weight: bold;
            margin-top: 8px;
        }

        footer {
            margin-top: 20px;
            padding: 10px;
            font-size: 0.9em;
            background: #e9f0ff;
        }
    </style>
</head>
<body>
    <header>
        🎧 Audio Guida - Equazioni di Primo Grado
    </header>

    <section id="teoria">
        <h2>1️⃣ Comprendere le Equazioni di Primo Grado</h2>
        <p>Un'equazione di primo grado è un'uguaglianza che contiene un'incognita elevata solo alla prima potenza.</p>
        <button onclick="riproduciAudio('intro')">🔊 Ascolta la spiegazione</button>
    </section>

    <section id="proprieta">
        <h2>2️⃣ Proprietà Fondamentali</h2>
        <p>Per risolvere un'equazione di primo grado possiamo usare due proprietà principali:</p>
        <ul style="text-align: left;">
            <li><b>Proprietà additiva:</b> possiamo addizionare o sottrarre lo stesso numero a entrambi i membri.</li>
            <li><b>Proprietà moltiplicativa:</b> possiamo moltiplicare o dividere entrambi i membri per lo stesso numero (diverso da zero).</li>
        </ul>
        <button onclick="riproduciAudio('proprieta')">🔊 Ascolta le proprietà</button>
    </section>

    <section id="esercizi">
        <h2>3️⃣ Esercizi - Scegli l'Equazione Corretta</h2>

        <div class="exercise">
            <p><b>Problema 1:</b> In una classe ci sono 8 studenti in più delle studentesse. In totale sono 28. Quale equazione rappresenta il problema?</p>
            <button onclick="verificaRisposta(this, true)">x + (x + 8) = 28</button>
            <button onclick="verificaRisposta(this, false)">x - 8 = 28</button>
            <button onclick="verificaRisposta(this, false)">2x = 28 + 8</button>
            <p class="feedback"></p>
        </div>

        <div class="exercise">
            <p><b>Problema 2:</b> Un libro e un quaderno costano insieme 15€. Il quaderno costa 3€ in meno del libro. Quale equazione usi?</p>
            <button onclick="verificaRisposta(this, false)">x + 3 = 15</button>
            <button onclick="verificaRisposta(this, true)">x + (x - 3) = 15</button>
            <button onclick="verificaRisposta(this, false)">2x = 15 - 3</button>
            <p class="feedback"></p>
        </div>

        <div class="exercise">
            <p><b>Problema 3:</b> Due fratelli hanno in totale 60€. Il maggiore ha il doppio dei soldi del minore. Quale equazione rappresenta il problema?</p>
            <button onclick="verificaRisposta(this, false)">x + 2 = 60</button>
            <button onclick="verificaRisposta(this, false)">x + x = 60</button>
            <button onclick="verificaRisposta(this, true)">x + 2x = 60</button>
            <p class="feedback"></p>
        </div>
    </section>

    <footer>
        © 2025 - Audio Guida Interattiva sulle Equazioni di Primo Grado
    </footer>

    <script>
        // Sintesi vocale per spiegazioni
        function riproduciAudio(tipo) {
            const synth = window.speechSynthesis;
            let testo = "";

            if (tipo === "intro") {
                testo = "Un'equazione di primo grado è un'uguaglianza che contiene un'incognita alla prima potenza. Ad esempio, due x più tre uguale sette.";
            } else if (tipo === "proprieta") {
                testo = "Per risolvere un'equazione di primo grado, possiamo applicare due proprietà. La proprietà additiva, che ci permette di sommare o sottrarre lo stesso numero a entrambi i membri, e la proprietà moltiplicativa, che consente di moltiplicare o dividere entrambi i membri per lo stesso numero, diverso da zero.";
            }

            const utterance = new SpeechSynthesisUtterance(testo);
            utterance.lang = "it-IT";
            utterance.rate = 1;
            synth.speak(utterance);
        }

        // Funzione per verificare le risposte
        function verificaRisposta(button, corretta) {
            const feedback = button.parentElement.querySelector(".feedback");
            if (corretta) {
                feedback.textContent = "✅ Corretto! Ottimo lavoro!";
                feedback.style.color = "green";
                riproduciFeedback("corretto");
            } else {
                feedback.textContent = "❌ Sbagliato! Riprova.";
                feedback.style.color = "red";
                riproduciFeedback("sbagliato");
            }
        }

        // Feedback sonoro per la risposta
        function riproduciFeedback(tipo) {
            const synth = window.speechSynthesis;
            let testo = tipo === "corretto" ? "Corretto! Complimenti!" : "Sbagliato. Riprova!";
            const utterance = new SpeechSynthesisUtterance(testo);
            utterance.lang = "it-IT";
            utterance.rate = 1;
            synth.speak(utterance);
        }
    </script>
</body>
</html>
